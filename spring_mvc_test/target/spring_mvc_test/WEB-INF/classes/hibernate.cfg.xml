<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property><!-- ou son nouveau
			nom com.mysql.cj.jdbc.Driver -->
        <property name="connection.url">jdbc:mysql://localhost:3306/you?useSSL=false&amp;serverTimezone=Africa/Casablanca</property>
        <property name="connection.username">root</property>
        <property name="connection.password"></property>

        <!-- dbcp connection pool configuration -->
        <property name="hibernate.dbcp.initialSize">5</property>
        <!--property name="hibernate.dbcp.maxTotal">20</property> <property name="hibernate.dbcp.maxIdle">10</property>
            <property name="hibernate.dbcp.minIdle">5</property> <property name="hibernate.dbcp.maxWaitMillis">-1</property -->

        <!-- Select our SQL dialect -->
        <!-- Type de la base de données -->
        <property name="dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>


        <!-- Enities to map -->
        <mapping class="org.example.entities.Users" />
        <mapping class="org.example.entities.Roles" />
        <mapping class="org.example.entities.Student" />
        <mapping class="org.example.entities.Administrator" />
        <mapping class="org.example.entities.Reservation" />
        <mapping class="org.example.entities.SignUp" />
        <mapping class="org.example.entities.TypeReservation" />


    </session-factory>
</hibernate-configuration>